{% extends "layout.njk" %}
{% from "govuk/components/table/macro.njk" import govukTable %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{%- from "moj/components/side-navigation/macro.njk" import mojSideNavigation -%}

{% set pageHeaderTitle = "View visits by date" %}
{% set pageTitle = applicationName + " - " + pageHeaderTitle %}
{% set backLinkHref = "/" %}

{% block content %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-full">
      <h1 class="govuk-heading-l">{{ pageHeaderTitle }}</h1>
    </div>

    <div class="govuk-grid-column-one-quarter">
        {{ mojSideNavigation({
        label: 'Slots navigation',
        classes: 'govuk-!-padding-top-0',
        sections: [
            {
                heading: {
                    text: 'Main visits room',
                    classes: 'govuk-!-padding-top-0'
                },
                items: [{
                    text: '11am to 12pm',
                    href: '/visits/',
                    active: true
                }, {
                    text: '3pm to 4pm',
                    href: '/visits'
                }]
            },
            {
                heading: {
                    text: 'Closed visits room'
                },
                items: [{
                    text: '3pm to 4pm',
                    href: '/visits'
                }]
            }
        ]
        }) }}

      <h3 class="govuk-heading-s">Visitor list</h3>
      <div class="govuk-button-group">
        {{ govukButton({
            text: "Download CSV"
        }) }}
        {{ govukButton({
            text: "Print",
            classes: "govuk-button--secondary"
        }) }}
      </div>

      <h3 class="govuk-heading-s">Prisoner list</h3>
      <div class="govuk-button-group">
        {{ govukButton({
            text: "Download CSV"
        }) }}
        {{ govukButton({
            text: "Print",
            classes: "govuk-button--secondary"
        }) }}
      </div>
    </div>

    <div class="govuk-grid-column-three-quarters">
      <h2 class="govuk-heading-m">X visits room, Y to Z</h2>
      <p>X of Y tables booked</p>
      <p>X visits in total (Y adults and Z children)</p>
      {{ govukTable({
        classes: "bapv-table govuk-!-margin-top-3",
        head: [
            {
              text: "Prisoner name"
            },
            {
              text: "Prison number"
            },
            {
              text: "Action",
              classes: "govuk-!-text-align-right"
            }
        ],
        rows: results
      }) }}
    </div>
  </div>
{% endblock %}
